// Programa una función que te devuelva el texto recortado según el número
//  de caracteres indicados, pe.miFuncion("Hola Mundo", 4) devolverá "Hola".

let palabra = "Hola Mundo";

if (palabra.length > 4) {
    console.log(`La longitud de la cadena es mayor a 4, 
    vendria siendo ${palabra.length}`);
} else {
    console.log(`La longitud de la cadena es ${palabra.length}`)
}

// console.log(palabra.slice(0,5))
    // slice cuenta como (0 el lado izquierdo muestra desde el inicio
    // , el lado derecho muetra desde el final )


// console.log(palabra.length(4));
